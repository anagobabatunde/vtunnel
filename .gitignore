# V build artifacts
*.o
*.so
*.dylib
*.dll
*.exe

# Built binaries (anchored to root so cmd/vtunnel/ source dir is not ignored)
/vtunnel
/vtunnel-server
/vtunnel-client

# V cache
.cache/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Binaries in root
/server
/client

# Build outputs in cmd directories
cmd/server/server
cmd/client/client
cmd/vtunnel/vtunnel

# Secrets — never commit tokens or keys
tokens.txt
*.pem
*.key

# Database files (hosted mode)
*.db
*.db-wal
*.db-shm

# Claude Code local settings (dev-only)
.claude/launch.json
.claude/settings.local.json

# Empty placeholder directories
tests/
scripts/
